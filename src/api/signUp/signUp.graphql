type Mutation {
	signUp(
		name: String!
		phone: String!
		password: String!
		email: String!
		gender: String!
		birth: String!
		bio: String
		companyName: String!
		companyRole: String!
		profileImg: String!
		cardImg: String
		geoLocation: String!
		loginSecret: String
		tags: String
	): User!
}
type Query {
	something: String
}
